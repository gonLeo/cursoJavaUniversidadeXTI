package br.com.xti.heranca;

//Dentro de uma interface todos os métodos são abstratos, mas não necessita declarar o modificador abstract
public interface AreaCalculavel {
    double calcularArea();
}
